#include <iostream>
#include <string>
#include "list.h"
#include "pila.h"
#include "cola.h"

using namespace std;

int main {
	lista<lista<string>*> *a;
	string aux

	while (aux != NULL) {
		cin.getline(aux, , ',');
		cout(aux);
	}
}